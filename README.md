# Foundation Playbook Finder

A self-contained React knowledge finder for operational teams. Search and inspect readiness playbooks without private credentials or external services.

## Requirements

- Node.js 20
- pnpm 10

## Commands

```bash
pnpm install --frozen-lockfile
pnpm test
pnpm lint
pnpm typecheck
pnpm build
```
